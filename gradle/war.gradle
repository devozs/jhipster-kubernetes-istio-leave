apply plugin: "war"

bootWar {
    mainClassName = "ro.trc.office.leave.LeaveApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]
}
